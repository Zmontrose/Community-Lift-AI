export default function App() {
  return (
    <div className="min-h-screen bg-slate-950 text-white">
      {/* Navigation */}
      <nav className="flex items-center justify-between px-8 py-6 border-b border-slate-800">
        <h1 className="text-2xl font-bold">
          CommunityLift AI
        </h1>

        <div className="space-x-6 hidden md:flex">
          <a href="#features">Features</a>
          <a href="#impact">Impact</a>
          <a href="#roadmap">Roadmap</a>
          <a href="#join">Join</a>
        </div>
      </nav>

      {/* Hero */}
      <section className="max-w-6xl mx-auto px-8 py-24 text-center">
        <div className="inline-block px-4 py-2 rounded-full bg-blue-500/20 text-blue-400 mb-6">
          Open Source Public Goods Platform
        </div>

        <h1 className="text-5xl md:text-7xl font-bold mb-6">
          Helping People
          <span className="text-blue-400"> Rebuild Their Future</span>
        </h1>

        <p className="text-xl text-slate-300 max-w-3xl mx-auto mb-8">
          Free AI-powered tools for resumes, storytelling,
          entrepreneurship, grant discovery, and career growth.
        </p>

        <div className="flex justify-center gap-4">
          <button className="bg-blue-600 px-6 py-3 rounded-lg hover:bg-blue-500">
            Launch App
          </button>

          <button className="border border-slate-700 px-6 py-3 rounded-lg">
            View GitHub
          </button>
        </div>
      </section>

      {/* Features */}
      <section
        id="features"
        className="max-w-6xl mx-auto px-8 py-20"
      >
        <h2 className="text-4xl font-bold text-center mb-12">
          Features
        </h2>

        <div className="grid md:grid-cols-3 gap-8">
          <FeatureCard
            title="AI Resume Builder"
            desc="Create professional resumes in minutes."
          />

          <FeatureCard
            title="Story Builder"
            desc="Turn personal experiences into impactful stories."
          />

          <FeatureCard
            title="Grant Finder"
            desc="Discover funding opportunities and resources."
          />

          <FeatureCard
            title="Business Generator"
            desc="Launch digital products and side businesses."
          />

          <FeatureCard
            title="Career Coach"
            desc="Get interview and job search assistance."
          />

          <FeatureCard
            title="Community Hub"
            desc="Connect with mentors and contributors."
          />
        </div>
      </section>

      {/* Impact */}
      <section
        id="impact"
        className="bg-slate-900 py-20"
      >
        <div className="max-w-6xl mx-auto px-8">
          <h2 className="text-4xl font-bold text-center mb-12">
            Impact Goals
          </h2>

          <div className="grid md:grid-cols-4 gap-8 text-center">
            <Stat number="5,000+" label="Users Served" />
            <Stat number="500+" label="Stories Published" />
            <Stat number="250+" label="Businesses Started" />
            <Stat number="100+" label="Jobs Obtained" />
          </div>
        </div>
      </section>

      {/* Roadmap */}
      <section
        id="roadmap"
        className="max-w-6xl mx-auto px-8 py-20"
      >
        <h2 className="text-4xl font-bold text-center mb-12">
          Roadmap
        </h2>

        <div className="grid md:grid-cols-4 gap-6">
          <RoadmapCard
            phase="Phase 1"
            text="Resume Builder MVP"
          />

          <RoadmapCard
            phase="Phase 2"
            text="Grant Discovery Engine"
          />

          <RoadmapCard
            phase="Phase 3"
            text="Community Mentorship"
          />

          <RoadmapCard
            phase="Phase 4"
            text="Mobile App Launch"
          />
        </div>
      </section>

      {/* CTA */}
      <section
        id="join"
        className="bg-blue-600 py-20 text-center"
      >
        <h2 className="text-4xl font-bold mb-4">
          Join CommunityLift AI
        </h2>

        <p className="mb-8 text-lg">
          Help build an open-source platform that expands
          access to opportunity.
        </p>

        <button className="bg-white text-black px-8 py-4 rounded-lg font-bold">
          Become a Contributor
        </button>
      </section>

      {/* Footer */}
      <footer className="text-center py-8 border-t border-slate-800">
        © 2026 CommunityLift AI · Founder: Montrose
      </footer>
    </div>
  );
}

function FeatureCard({ title, desc }) {
  return (
    <div className="bg-slate-900 p-6 rounded-xl border border-slate-800">
      <h3 className="text-xl font-bold mb-3">{title}</h3>
      <p className="text-slate-400">{desc}</p>
    </div>
  );
}

function Stat({ number, label }) {
  return (
    <div>
      <div className="text-5xl font-bold text-blue-400">
        {number}
      </div>
      <div className="mt-2 text-slate-300">{label}</div>
    </div>
  );
}

function RoadmapCard({ phase, text }) {
  return (
    <div className="bg-slate-900 p-6 rounded-xl border border-slate-800">
      <h3 className="font-bold mb-2">{phase}</h3>
      <p>{text}</p>
    </div>
  );
}# For most projects, this workflow file will not need changing; you simply need
# to commit it to your repository.
#
# You may wish to alter this file to override the set of languages analyzed,
# or to provide custom queries or build logic.
#
# ******** NOTE ********
# We have attempted to detect the languages in your repository. Please check
# the `language` matrix defined below to confirm you have the correct set of
# supported CodeQL languages.
#
name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '16 5 * * 5'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    # Runner size impacts CodeQL analysis time. To learn more, please see:
    #   - https://gh.io/recommended-hardware-resources-for-running-codeql
    #   - https://gh.io/supported-runners-and-hardware-resources
    #   - https://gh.io/using-larger-runners (GitHub.com only)
    # Consider using larger runners or machines with greater resources for possible analysis time improvements.
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      # required for all workflows
      security-events: write

      # required to fetch internal or private CodeQL packs
      packages: read

      # only required for workflows in private repositories
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        # CodeQL supports the following values keywords for 'language': 'actions', 'c-cpp', 'csharp', 'go', 'java-kotlin', 'javascript-typescript', 'python', 'ruby', 'rust', 'swift'
        # Use `c-cpp` to analyze code written in C, C++ or both
        # Use 'java-kotlin' to analyze code written in Java, Kotlin or both
        # Use 'javascript-typescript' to analyze code written in JavaScript, TypeScript or both
        # To learn more about changing the languages that are analyzed or customizing the build mode for your analysis,
        # see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning.
        # If you are analyzing a compiled language, you can modify the 'build-mode' for that language to customize how
        # your codebase is analyzed, see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/codeql-code-scanning-for-compiled-languages
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # Add any setup steps before running the `github/codeql-action/init` action.
    # This includes steps like installing compilers or runtimes (`actions/setup-node`
    # or others). This is typically only required for manual builds.
    # - name: Setup runtime (example)
    #   uses: actions/setup-example@v1

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # If you wish to specify custom queries, you can do so here or in a config file.
        # By default, queries listed here will override any specified in a config file.
        # Prefix the list here with "+" to use these queries and those in the config file.

        # For more details on CodeQL's query packs, refer to: https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
        # queries: security-extended,security-and-quality

    # If the analyze step fails for one of the languages you are analyzing with
    # "We were unable to automatically build your code", modify the matrix above
    # to set the build mode to "manual" for that language. Then modify this step
    # to build your code.
    # ℹ️ Command-line programs to run using the OS shell.
    # 📚 See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun
    - name: Run manual build steps
      if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        echo 'If you are using a "manual" build mode for one or more of the' \
          'languages you are analyzing, replace this with the commands to build' \
          'your code, for example:'
        echo '  make bootstrap'
        echo '  make release'
        exit 1

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
